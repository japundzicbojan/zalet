# Official hackathon partner stack
FIRECRAWL_API_KEY=
EXA_API_KEY=
XAI_API_KEY=
FAL_KEY=
DAYTONA_API_KEY=
NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOY_KEY=

# Optional
# Absolute data root (Render disk). Defaults to ./.data
ZALET_DATA_DIR=
# Required as x-zalet-token for GET /api/runs listing
ZALET_RUN_SECRET=
# Keep Daytona sandbox alive for remote preview (default: delete after pack)
DAYTONA_KEEP_SANDBOX=
# Optional Grok model override
XAI_MODEL=
